ouvrir les paramètres Windows
taper fonctionnalités Windows dans la barre de recherche
cliquer sur Activer ou désactiver des fonctionnalités Windows
Cocher Sous-système Windows pour Linux
OK
redémarrer le PC

ouvrir le Microsoft Store
rechercher Ubuntu
l'installer
l'ouvrir
Fermer lorsqu'un nom d'utilisateur est demandé

Rechercher cmd dans la barre de recherche
Exécuter l'invite de commande en tant qu'administrateur
taper ubuntu install --root	// pour sauter la création d'un compte utilisateur lorsqu'on ouvre Ubuntu (laisse l'utilisateur par défaut en root)
Fermer l'invite de commande

Lancer à nouveau Ubuntu en tant qu'administrateur
apt-get update			// télécharge les mises à jour des packages (paquets)
apt-get upgrade			// met à jour les packages vers leurs dernières versions
y enter				// valider
apt-get dist-upgrade		// vérifie les dépendances et supprime les packages obsolètes du système
snap refresh			// Vérifie, télécharge et installe les mises à jour Snap